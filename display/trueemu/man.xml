<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE refentry PUBLIC
  "-//OASIS//DTD DocBook XML V4.1.2//EN"
  "http://www.oasis-open.org/docbook/xml/4.1.2/docbookx.dtd">
<refentry id="ref-display-trueemu">

<refmeta>
    <refentrytitle>display-trueemu</refentrytitle>
    <manvolnum>7</manvolnum>
    <refmiscinfo>GGI</refmiscinfo>
</refmeta>

<refnamediv>
    <refname id="display-trueemu">display-trueemu</refname>
    <refpurpose>Emulate truecolor modes</refpurpose>
</refnamediv>

<refsynopsisdiv>
<cmdsynopsis>

    <command>display-trueemu</command>
    <arg choice="plain">:</arg>
    <arg><option>-parent=<replaceable>mode</replaceable></option></arg>
    <arg><option>-dither=<replaceable>dither</replaceable></option></arg>
    <arg><option>-model=<replaceable>model</replaceable></option></arg>
    <arg choice="req"><replaceable>target-spec</replaceable></arg>

</cmdsynopsis>
</refsynopsisdiv>


<refsect1><title>Description</title>

<para>
    Emulates truecolor modes (<symbol>GT_TRUECOLOR</symbol>) on another target which
    can only do palette modes or truecolor modes of different sizes/depths.
    Uses dithering and special palettes to get the highest possible quality.
</para>

</refsect1>

<refsect1><title>Options</title>

    <variablelist>

    <varlistentry>
    <term><option>-parent=<replaceable>mode</replaceable></option></term>
    <listitem><para>
    Force the parent target to use a specific mode, specified as a standard 
    LibGGI mode string (see <xref linkend="ref-libggi"/>).
    </para></listitem>
    </varlistentry>

    <varlistentry>
    <term><option>-dither=<replaceable>dither</replaceable></option></term>
    <listitem><para>
    Specifies the amount of dithering.  Legal values are 0, 2 and 4,
    defaulting to 4.
    </para>
    <para>
    When the target is running,
    <keycombo><keycap>Ctrl</keycap><keycap>Alt</keycap><keycap>d</keycap></keycombo>
    changes the dithering level.
    </para></listitem>
    </varlistentry>

    <varlistentry>
    <term><option>-model=<replaceable>model</replaceable></option></term>
    <listitem><para>
    Color model to use (only when the parent is palettized).  Legal
    values are rgb, cube and pastel.  Default depends on the parent
    mode.
    </para>
    <para>
    When the target is running,
    <keycombo><keycap>Ctrl</keycap><keycap>Alt</keycap><keycap>p</keycap></keycombo>
    changes the current color model.
    </para></listitem>
    </varlistentry>

    <varlistentry>
    <term><replaceable>target-spec</replaceable></term>
    <listitem><para>
    Specifies the target which to draw on (the `parent'
    target).  This defaults to automatic selection (just like using
    <function>ggiOpen</function>(<symbol>NULL</symbol>) when
    <envar>GGI_DISPLAY</envar> is not set).
    </para></listitem>
    </varlistentry>

    </variablelist>
    
</refsect1>

<refsect1><title>Features</title>
  
<simplelist>
<member>DirectBuffer never supported.</member>
<member>Unaccelerated.</member>
</simplelist>

</refsect1>

</refentry>
